# build/ — Ressourcen für electron-builder

**icon.ico** hier ablegen (256×256 empfohlen, .ico mit mehreren Größen).
Ohne Icon baut electron-builder mit dem Standard-Electron-Icon (nur eine Warnung).

Icon erzeugen: PNG → https://icoconvert.com oder `npx png-to-ico logo.png > icon.ico`
